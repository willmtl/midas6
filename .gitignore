# Data & secrets — never commit
.data/
*.env
.env
.env.*
**/.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Node / frontend build
node_modules/
frontend/node_modules/
frontend/build/

# Editor / OS
.DS_Store
*.swp

# Tooling caches (keep project config, drop local state)
.claude/

# Frontend is a self-managed git repo (own .git) — versioned separately
/frontend/
