# NOT USED. docker-compose bind-mounts the REPO-ROOT ./fetch_delisted.py to /app/fetch_delisted.py
# (individual file mount overrides the backend/->/app dir mount for this path). Edit the root copy,
# not this one. This stub exists only so the dir mount has something here.
